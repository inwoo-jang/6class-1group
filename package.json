{
  "name": "6class-1group",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "api": "node mock-api/server.js",
    "test": "node --test",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "VITE_OPENWEATHER_API_KEY= npm run build && cp dist/index.html dist/404.html && touch dist/.nojekyll && npx --yes gh-pages@6 -d dist -b gh-pages --dotfiles -m \"deploy\""
  },
  "dependencies": {
    "axios": "^1.19.0",
    "element-plus": "^2.14.3",
    "leaflet": "^1.9.4",
    "openai": "^7.4.0",
    "pinia": "^4.0.2",
    "vue": "^3.5.40",
    "vue-router": "^5.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "vite": "^8.1.5"
  }
}
